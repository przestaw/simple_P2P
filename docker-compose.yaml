version: "3.5"

services:
  host:
    stdin_open: true
    tty: true
    build:
      context: .
      network: host
    networks:
      - simple_p2p_network

networks:
  simple_p2p_network:
    driver: bridge
    # ipam:
    #   driver: default
    #   config:
    #     - subnet: ip/mask
